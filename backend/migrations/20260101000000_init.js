export function up(knex) {
  // This migration was previously run but the file was removed.
  // Placeholder to satisfy Knex validation — no-op.
  return Promise.resolve()
}

export function down(knex) {
  return Promise.resolve()
}
